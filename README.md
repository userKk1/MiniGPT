# MiniGPT

A from-scratch GPT-style transformer language model, trained on Python code, implementing tokenization, self-attention, and the full training pipeline without relying on pre-built model libraries.